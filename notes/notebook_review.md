# Notebook 整理紀錄

原始 ddpm_experiments.ipynb（14 個程式儲存格）保持不變。新增 ddpm_experiments_review.ipynb 作為閱讀與重跑入口。

觀察到重複 clone 的巢狀路徑、重複環境與舊繪圖、訓練保護觸發的 RuntimeError，以及權重檢查格後貼入舊結果 JSON。整理版使用固定來源版本與絕對路徑，分開展示、訓練和評估；預設不啟動 GPU 工作，既有 checkpoint 不自動續訓。

原始第 7 格（索引 6）保存新增模型取樣 seed=123 的九組完成輸出；第 12、14 格保存權重完成步數／metadata 輸出。這些是保存的執行證據，不是本次重跑。

整理版由原始訓練與消融程式改寫成函式，移除重複操作、清空其輸出與計數，加入說明。變更了結果版本與來源欄位，因此使用獨立結果目錄，不混入歷史結果。取樣更新公式保持原樣。

驗證範圍：JSON 結構、儲存格順序、預設旗標、原始取樣公式與文字對照。當前無 Python／Colab 執行環境，未執行語法檢查器、nbformat validator 或 GPU 工作流；不宣稱已通過執行測試。
